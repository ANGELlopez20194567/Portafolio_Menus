# Back link

Estilo compartido para el enlace “Volver a RayelTech” de todas las demos. Cada proyecto importa `back-link.css` y usa las clases `back-link`, `back-label` y `back-arrow`; así se mantiene la misma apariencia y comportamiento responsive sin duplicar reglas.
